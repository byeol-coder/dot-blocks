# Changelog

## 퍼즐 트레이 레이아웃·슈퍼닷 TTS 검수 보강

- `renderMini()`가 셀 사이 간격을 포함하지 않고 크기를 계산해 세로 3칸·3×3 계열 블록이
  트레이 카드 밖으로 시각적으로 새던 문제를 수정
- 퍼즐 모드의 `놓을 블록` 트레이에서 3×3·지그재그 미니 블록이 이름/설명 텍스트를 침범하지 않도록
  프리뷰 슬롯과 버튼 높이를 실제 최대 미니어처 크기에 맞춰 고정
- 단독 실행에서는 슈퍼닷 TTS 코어가 없는 서버 `/api/tts`를 먼저 시도하지 않도록 브라우저 음성 우선으로 설정
- 데스크톱·태블릿·좁은 화면·모바일 뷰포트에서 트레이 겹침, 문서 오버플로우, 콘솔 에러를 재검수

## 인트로 배경 소스 교체

- 게임 진입 인트로 배경을 새 `intro-bg.png` 소스로 교체하고, 기존 CSS 계약에 맞춰
  `assets/intro-bg.webp`와 모바일용 `assets/intro-bg-mobile.webp`를 다시 생성
- 새 배경 구성에 맞춰 자산 설명 문서의 인트로 배경 설명을 갱신

## 닷패드 기능키 감사 — 미연결 진입점 수정

SDK 3.0.0이 실제로 노출하는 키는 `KeyFunction1~4`, `KeyFunction12/13/14/23/24/34`, `KeyElse`,
`PanningLeft/Right/All`, `LPF1`, `RPF4` 16종입니다. 코드가 참조하던 `LPF4`는 **정의되지 않은 키**라
플레이 중 설정 메뉴로 들어가는 경로가 동작하지 않았습니다(가드 덕에 오류는 없었음).

- 플레이 중 설정 진입을 **F1+F4**로 연결. 두 모드 모두 비어 있던 키이고, 바깥쪽 두 손가락이라
  오입력 위험이 낮을 것으로 보이나, 실제 펌웨어가 F1+F4를 F1 또는 F4 단독 입력으로 오인하지
  않는지는 실기 확인 필요. `LPF4` 검사는 해당 키를 노출하는 펌웨어를 위해 남겨둠
- 설정 시트의 기능키 안내문 한/영 4종에 `F1+F4 설정` 추가

## 블라인드-퍼스트 P0를 소스에 반영하고 런타임 재작성기 제거

`blind-first-p0.js`는 index.html을 XHR로 가져와 문자열 치환한 뒤 `document.write`로 문서를
다시 쓰는 런타임 업그레이드였습니다. **파서가 살아 있는 동안 호출된 `document.open()`은 사양상
무시되므로, 성공해도 문서를 교체하지 않고 덧붙여 DOM이 두 벌이 됩니다.** 실제로는 needle 충돌로
매번 실패해 조용히 건너뛰고 있었고, 그래서 아래 기능들이 라이브에 올라간 적이 없습니다.
치환 결과를 index.html에 직접 반영하고 스크립트를 삭제했습니다.

이번에 실제로 활성화된 P0 기능:

- **음성 안내 방식 선택** (슈퍼닷 TTS / 스크린리더만 / 둘 다). 슈퍼닷 TTS 사용 시 `aria-live`를
  꺼서 합성음과 스크린리더가 겹쳐 읽는 것을 막음
- **촉각 저속·촉각 턴제 낙하 방식 추가**, 기본값을 촉각 저속(3초)으로 변경. 턴제는 중력을 멈추고
  바닥에 닿으면 확정 방법을 음성으로 안내
- **되돌릴 수 없는 동작에 2회 확인** — 즉시 낙하와 다시 시작은 1.8초 안에 같은 키를 두 번 눌러야
  실행. 키보드 `Space`·`N`·`Enter`, 터치 스와이프, 닷패드 F2+F3·오른쪽 패닝 모두 적용
- **닷패드만으로 설정 변경** — LPF4 또는 인트로 F4로 설정 메뉴 진입, F1/F2 이동, F3 변경, F4 닫기.
  7개 항목을 순서·값·조작법과 함께 음성 안내
- **인트로를 닷패드로 조작** — F1 낙하 모드, F2 블록 퍼즐, F3 도움말, F4 설정
- **촉각 부호 개선** — 고정 블록은 작고 촘촘한 면, 움직이는 블록은 큰 빈 테두리로 대비를 키우고,
  줄 삭제 시 해당 행을 가득 채운 플래시 프레임 추가
- 닷패드 연결 시 세로 거치와 인트로 기능키를 함께 안내

- `tts.js`는 이제 음성 코어만 로드. 안전을 위해 `<html data-blind-first-p0>` 마커는 남겨둠
  (구버전 `tts.js`가 캐시된 방문자가 옛 스크립트를 불러도 마커를 보고 즉시 빠져나감)
- superdot-tts-core의 서버/브라우저 음성 선택도 호스트네임 대신 프레임 판정으로 전환

## 블록 타이틀 · 콘솔 에러 정리

- **인트로 타이틀을 조립 블록으로 재구성** — `DOT BLOCKS` 9글자를 색 타일로 배치. 실제 텍스트라
  확대·번역·고대비가 그대로 동작하고, 타일마다 우하단에 닷패드 핀 모티프를 넣음
  - 낱글자는 전부 `aria-hidden`, `h1`은 `.sr-only` "닷 블록스, DOT BLOCKS" 하나만 낭독
    (기존에는 영문이 그대로 읽혀 제목이 두 번 들렸음)
  - **글자색은 흰색이 아니라 다크 잉크** — 흰 글자는 노랑 타일에서 1.46:1이고 9개 중 4개가 큰 텍스트
    기준 3:1도 미달. 타일 색상값은 그대로 두고 글자만 바꿔 3.9~13.5:1 확보
  - 720px 이하에서는 DOT / BLOCKS 두 줄로 분리
- `.sr-only`에 `clip-path: inset(50%)` 추가, `prefers-contrast: more` 대응 추가
- **`DotBlocksAudioEngine is not a constructor` 해결** — `audio-engine.js`를 tts 체인보다 먼저 로드.
  `blind-first-p0.js`가 `document.write`로 문서를 다시 쓰면 스크립트가 재실행되면서 인라인 게임
  스크립트가 엔진보다 먼저 돌 수 있는데, 전역은 `document.open()`을 넘어 살아남으므로 먼저 정의해두면
  재작성 횟수와 무관하게 안전. 엔진이 없을 때 무음으로 계속 실행되는 방어 코드도 추가
- **불필요한 404 제거** — `/tts.js`와 `/api/tts`는 닷게임즈 호스트에만 있고, 호스트에서는 항상
  iframe 안에서 실행됨. 호스트네임 매칭 대신 프레임 여부로 판별하도록 바꿔 GitHub Pages뿐 아니라
  모든 단독 배포에서 요청 자체가 나가지 않도록 함

## 화면 구조 재설계 · 닷게임즈 공통 규칙 준수

- **세로 Flex 스크린 아키텍처로 전환** — 인트로와 플레이 모두 `상단 고정 → 가운데가 남는 높이를
  차지하며 정중앙 정렬 → 하단 고정` 구조. 컨트롤 개수나 상태 문구 길이가 바뀌어도 보드 위치가
  흔들리지 않음. 배경은 `.screen-bg` 전용 레이어(z-index -3)로 분리
  - 데스크톱 3열(다음·보관 / 보드 / 기록), 820px 이하는 좌우 패널을 숨기지 않고 보드 위 가로 HUD로 재배치
  - 하단에 조작 안내 레일(`.play-help`) 추가
- **보드 베젤을 배경에서 요소로 승격** — 새 플레이 아트워크의 안쪽 창이 398×816으로 보드의 1:2와
  같아, 화면 배경(`cover`)으로 두면 뷰포트 비율마다 캔버스와 어긋남. 베젤만 잘라
  `assets/board-frame.webp`로 만들고 보드 셸에 직접 적용. 프레임 비율 `473 / 889` + 안쪽 창에서
  환산한 퍼센트 패딩(`7.61% 7.82% 7.82% 8.03%`)으로 어떤 크기에서도 정합
  - 플레이 배경은 CSS 라디얼 + 도트 패턴으로 생성해 인트로와 시각적으로 구분
  - 미사용이 된 `play-bg.webp`·`play-bg-mobile.webp`·`panel-bg.webp` 삭제
- **소리 토글 추가** (닷게임즈 5개 게임 공통 컴포넌트) — 인트로와 컨트롤 도크에 노출. 음성·방향
  효과음·배경음악을 한 번에 끄고, 끄기 직전 상태를 기억했다가 복원. 설정 서랍의 개별 스위치와
  양방향 동기화. 아이콘·`aria-pressed`·라벨 문구는 참고 게임과 동일
- **호스트 메시지 계약 구현** — `postMessage({source:'dotarcade', game:'dot-blocks', type, …})`로
  `ready` / `resize` / `gameStart` / `gameEnd` / `exit` 송신, `tw:pause` / `tw:resume` /
  `tw:setLang` 수신. 부모 프레임이 없으면 전부 무시
- **호스트 TTS 우선 로드** — 루트 `/tts.js`를 로컬 로더보다 먼저 불러 플랫폼 `TW_TTS`를 사용.
  없으면 기존 `superdot-tts-core.js`가 이어받음(해당 파일에 이미 중복 정의 가드 있음)
- **대비 보정** — 프라이머리 버튼의 흰 라벨이 액센트 `#ff5b22` 위에서 3.1:1(hover 2.66:1)로 AA
  미달이라, 액센트 색상값은 유지하고 라벨을 다크 잉크(`#140801`)로 변경해 6.36:1 / 7.4:1 확보
- 최소 터치 영역을 전 컨트롤 44px로 통일

## 기능키만으로 블록 확정 · 타이틀 정비 · 점자 표시줄 확장

- **F2+F3에 「내려놓기」 배치** — 블록을 확정하는 동작이 낙하 모드는 오른쪽 패닝, 퍼즐 모드는
  전체 패닝에만 있어서, 손이 F1~F4에 올라가 있는데 확정할 때마다 패닝키로 옮겨야 했음.
  가운데 인접 쌍(검지+중지)이라 손 위치를 바꾸지 않고 누를 수 있고, 두 모드에서 같은 제스처가
  같은 의미를 갖도록 통일. **기존 패닝키 매핑은 그대로 유지**해 기존 사용자 조작이 깨지지 않음
- 히어로 타이틀을 게임 타이틀 화면 형태로 변경: **DOT BLOCKS** + 닷 블록스(보조) + 태그라인
  - 한글 음차는 `aria-hidden` 처리해 스크린리더가 제목을 두 번 읽지 않음 (h1 낭독 = "DOT BLOCKS")
- **낙하 모드 점자 라인 확장** `[열] [착지까지 칸]` → `[현재 블록] [열] [착지까지 칸] [다음 블록]`
  - 표준 라틴 점자 I O T S Z J L 사용. T·S·Z·J·L은 기존 음성 안내(티/에스/지/제이/엘)와 일치
  - 점 배치는 파일의 기존 상수 6개(수표·o·x·숫자)와 비트 레이아웃 대조로 검증. 20칸 중 11칸 사용
- 설정에 「점자 표시줄」 안내 카드 추가 (모드별 표기 순서, 한/영)
- 블록 색상은 기존 배색을 유지 (표준 테트리스 배색 적용안은 반영하지 않음)

## 닷패드 숙련자 대응 강화 (Dot 틱택토 정보설계 참고)

- **점자 라인을 실시간 위치 표시로 전환** — 20셀 점자 라인은 읽는 손가락 바로 아래에 있는데
  기존에는 점수·줄수만 띄우고 있었음. Dot 틱택토가 커서 위치와 칸 내용을 실시간으로 보여주는
  방식을 참고해, 낙하 모드는 `#열 #착지까지칸수`, 블록 퍼즐은 `#행 #열 o|x`(o=놓을 수 있음)로 변경.
  이동할 때마다 갱신되므로 음성 없이도 지연 0으로 위치 파악 가능
- **점자 숫자 0 인코딩 버그 수정** — `0x35`(1·3·5·6점)로 잘못 매핑되어 있던 것을 표준 `0x1A`(2·4·5점)로
  교정. 점수가 0에서 시작하므로 사용자는 항상 틀린 글자를 읽고 있었음
- **상태 음성을 2단계로 분리** — 기존 왼쪽 패닝 상태 안내가 약 110자로 길어 반복 청취에 부담.
  간단(왼쪽 패닝/S) 22자로 축약, 상세(F3+F4/D)에 점수·다음·열 높이·빈칸 전부 유지
- **다음·보관 전용 키 추가** — 간단 티어에서 빠진 정보를 미사용 `F2+F4`(키보드 V)에 배치, 19자
- 조작법 안내(닷패드 기능키·키보드)와 진단 API `getAccessibilityState().brailleLine` 갱신

## 영문 모드 크래시 수정

- **`#subtitle` TypeError 수정**: `applyLang()`이 존재하지 않는 `#subtitle` 엘리먼트에 접근해
  중단되던 문제. 이 때문에 이후에 나열된 영문 번역 대부분이 적용되지 않고 있었음
- **모드 카드 파괴 버그 수정**: `dropModeBtn.textContent`/`puzzleModeBtn.textContent` 할당이
  버튼 내부의 아이콘·제목·설명 자식 엘리먼트를 전부 지워버려, 이후 `#dropModeName` 등 접근에서
  다시 중단되던 문제
- 누락돼 있던 `introDpLabel`, `pinLabel` 영문 문구 추가
- 결과: 인트로·플레이·설정·일시정지 전 화면 영문 모드 한글 잔존 0건, 페이지 에러 0건

## GitHub Pages 음성 안내 지연 제거

- `TW_TTS`는 `/api/tts` 서버 엔드포인트를 우선 시도하는데, 이 엔드포인트는 Dot Games 호스트
  플랫폼에만 존재함. GitHub Pages(`*.github.io`)나 `file://`로 열면 매번 404 후 브라우저
  음성으로 폴백하느라 첫 안내까지 약 272ms가 밀림
- `speak()`에 호스트 판별을 추가해 `github.io`/`file://`에서는 `TW_TTS`를 건너뛰고
  `speechSynthesis`로 직행 (첫 음성까지 272ms → 40ms). 그 외 호스트(닷게임즈 플랫폼 등)는
  기존과 동일하게 `TW_TTS` 사용 — dot-minesweeper/dotfootsteps와 같은 패턴
- 음성이 이 게임의 주 피드백 채널이라 안내마다 밀리는 지연이 특히 중요했음

## 순수 배경 적용 · 인트로 문구 축약

- 텍스트·버튼이 구워지지 않은 순수 배경 5종으로 교체 (인트로/플레이 각 가로·세로, 설정·결과용)
- 인트로 모바일 배경은 빈 공간이 **상단**이라 문구를 상단 정렬로 변경하고 스크림 방향도 반전 (기존 하단 정렬은 그림과 겹쳤음)
- 플레이 배경이 중앙부가 밝아 `.stage::before` 스크림을 단색에서 그라데이션으로 교체, 모바일은 중간 밝은 띠에 맞춰 별도 가중
- 설정 서랍·일시정지·결과 오버레이에 `panel-bg.webp` 적용 (밝은 쪽이 왼쪽, 서랍은 오른쪽 어두운 면에 위치)
- 인트로 문구 136자 → 76자로 축약 (아이브로우 16→8, 설명 76→27, 키 안내 44→20)
- 블록 색상은 테트리스 표준 배색과 겹치지 않도록 기존 독자 팔레트를 그대로 유지

## 레트로 칩튠 배경음악 프리셋 추가

- 설정 서랍에 「배경음악 스타일」 선택 추가 — 기본(앰비언트) / 레트로(칩튠) 중 선택, localStorage에 저장
- 레트로 프리셋은 퍼블릭 도메인 러시아 민요 「코로베이니키」(1861) 선율을 자체 편곡한 8마디 128스텝 루프
- 사각파 멜로디 + 삼각파 루트·5도 교대 워킹 베이스 + 오프비트 하이햇으로 구성
- 기존 절차형 엔진의 템포·위험도 반응(레벨/danger에 따라 최대 146BPM까지 가속)을 그대로 계승
- 블록 퍼즐 모드에서는 퍼커션을 빼고 삼각파로 부드럽게 연주
- 시각장애인 사용자의 방향 효과음·음성 안내를 가리지 않도록 음악 게인을 효과음보다 낮게 유지하고 기존 TTS 자동 감쇠 그대로 적용
- `audio-engine.js` 스케줄러 루프 길이를 프리셋별로 가변 처리 (기본 32스텝 / 레트로 128스텝)
- `AUDIO-LICENSE.md`에 멜로디 출처·편곡 창작성·상표 비관련 고지 명시

## 아케이드 게임감 강화 (라인 클리어·콤보·레벨업 연출)

- 라인 클리어 시 "N줄 클리어 / 콤보 ×N / 테트리스! / 레벨 N!" 팝업 콜아웃 추가 (Drop 모드·Puzzle 모드 모두)
- 3줄 이상 동시 클리어 시 보드 화면 흔들림 효과 추가
- 하드 드롭 착지 시 보드에 미세한 임팩트 바운스 추가
- 점수·줄·레벨·최고 기록 HUD 숫자가 바뀔 때 팝 애니메이션으로 강조
- 다음/보관/트레이 블록 미리보기가 갱신될 때 팝인 애니메이션 추가
- 모든 신규 연출은 `prefers-reduced-motion`에서 자동 비활성화되고, 기존 점수 계산·TTS 안내·오디오 타이밍은 변경 없이 유지

## UI/UX 세련화 패스 (톤앤매너·인터랙션·레이아웃)

- CTA 그라데이션 3곳이 서로 다른 값으로 드리프트되어 있던 것을 `--cta-1`/`--cta-2`/`--cta-glow` 토큰 하나로 통일 (인트로 시작 버튼, 플레이 헤더 로고, 오버레이 확인 버튼)
- 반복되던 플레이 화면 패널/보더 블루를 `--panel-deep`, `--panel-deep-2`, `--line-blue`, `--line-blue-soft` 토큰으로 정리
- 화이트 텍스트 대비 CTA 그라데이션 시작색 대비를 2.91:1 → 3.84:1로 개선 (WCAG AA 큰글자 기준 충족)
- 일시정지·결과 오버레이의 유니코드 글리프(Ⅱ, ★)를 기존 아이콘 시스템과 통일된 인라인 SVG(일시정지, 트로피)로 교체
- 홈/메뉴/드로어 닫기 버튼과 기본 `.btn`, 플레이 컨트롤바 버튼을 44px 터치 타깃 기준으로 상향
- 버튼·모드 카드·토글·트레이 조각·드로어 닫기에 hover/press 트랜지션 추가 (기존에는 트랜지션이 전무했음)
- 인트로/플레이 화면, 설정 드로어, 일시정지·결과 오버레이에 `[hidden]` 기반 자동 진입 애니메이션 추가 (JS 변경 없이 CSS만으로 구현)
- reduced-motion 리셋 규칙이 참조만 하고 정작 구현되지 않았던 위험 단계 상태 점 펄스 애니메이션을 신규 구현
- `prefers-reduced-motion` 규칙을 새 애니메이션까지 포괄하도록 확장

## Screen-separated UI edition

- 인트로, 플레이, 일시정지, 결과 화면을 독립된 UI 상태로 분리
- 플레이 화면에서 조작법과 설정을 제거하고 핵심 점수·보드·4개 버튼만 유지
- 조작법, 닷패드 기능키, 오디오 및 접근성 설정을 오른쪽 설정 서랍으로 이동
- 인트로와 플레이 배경을 WebP 이미지 자산으로 연결
- 기본 블록을 Canvas 기반 셀 조합으로 유지하고 입체 하이라이트 렌더링 개선
- 별, 폭탄, 무지개, 랜덤 특수 아이템 SVG 자산 추가
- 모바일 모드 카드, 세로 보드, 퍼즐 트레이, 하단 핵심 컨트롤 반응형 재구성
- 홈, 설정, 일시정지, 재시작, 결과 흐름과 키보드 포커스 이동 추가


## Blind-first polished edition

- 시각장애인 사용자 기준으로 촉각 부호 재설계
- 고정/현재/착지/배치불가 블록을 서로 다른 핀 질감으로 구분
- 촉각 표면 스캔과 열 높이 상세 음성 추가
- 스테레오 방향 및 높이 기반 이어콘 추가
- CC0 Web Audio 절차형 배경음악 추가
- 음성 중 BGM 자동 감쇠
- 음성/효과음/BGM 개별 설정 추가
- 촉각 친화 잠금 유예와 3단계 속도 추가
- 블록 보관, 7-bag, 콤보 점수 추가
- 위험 높이 2단계 경고 추가
- 모바일 스와이프 조작 추가
- 백그라운드 전환 시 자동 일시정지
- 동적 Canvas aria-label 및 assertive 위험 알림 추가
- 진단 API `getAccessibilityState()` 추가
